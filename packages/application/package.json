{
  "name": "@eaw/application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "prepare:domain": "pnpm --filter @eaw/domain run build",
    "prepare:pricing": "pnpm --filter @eaw/calculation-engine run build && pnpm --filter @eaw/pricing-engine run build",
    "prepare:platform": "pnpm --filter @eaw/platform-engine run build",
    "prepare:rules": "pnpm --filter @eaw/rule-engine run build",
    "prepare:promotion": "pnpm --filter @eaw/promotion-engine run build",
    "prepare:ai": "pnpm --filter @eaw/ai-engine run build && pnpm --filter @eaw/workspace run build",
    "prepare:competitors": "pnpm --filter @eaw/competitor-engine run build",
    "prepare:workflows": "pnpm --filter @eaw/workflow-engine run build",
    "prebuild": "pnpm run prepare:domain && pnpm run prepare:pricing && pnpm run prepare:platform && pnpm run prepare:rules && pnpm run prepare:promotion && pnpm run prepare:ai && pnpm run prepare:competitors && pnpm run prepare:workflows",
    "build": "tsc -p tsconfig.json",
    "pretest": "pnpm run prepare:domain && pnpm run prepare:pricing && pnpm run prepare:platform && pnpm run prepare:rules && pnpm run prepare:promotion && pnpm run prepare:ai && pnpm run prepare:competitors && pnpm run prepare:workflows",
    "test": "vitest run src",
    "pretypecheck": "pnpm run prepare:domain && pnpm run prepare:pricing && pnpm run prepare:platform && pnpm run prepare:rules && pnpm run prepare:promotion && pnpm run prepare:ai && pnpm run prepare:competitors && pnpm run prepare:workflows",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src"
  },
  "dependencies": {
    "@eaw/ai-engine": "workspace:*",
    "@eaw/calculation-engine": "workspace:*",
    "@eaw/competitor-engine": "workspace:*",
    "@eaw/domain": "workspace:*",
    "@eaw/pricing-engine": "workspace:*",
    "@eaw/promotion-engine": "workspace:*",
    "@eaw/platform-engine": "workspace:*",
    "@eaw/prompt-engine": "workspace:*",
    "@eaw/rule-engine": "workspace:*",
    "@eaw/workflow-engine": "workspace:*",
    "@eaw/workspace": "workspace:*"
  }
}
