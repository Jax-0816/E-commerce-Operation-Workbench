{
  "name": "@eaw/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:shared": "pnpm --filter @eaw/shared run build",
    "prebuild": "pnpm run prepare:shared",
    "predev": "pnpm run prepare:shared",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "prestart": "pnpm run prepare:shared",
    "start": "node dist/index.js",
    "pretest": "pnpm run prepare:shared",
    "test": "vitest run src",
    "pretypecheck": "pnpm run prepare:shared",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src"
  },
  "dependencies": {
    "@eaw/shared": "workspace:*",
    "@fastify/static": "8.3.0",
    "fastify": "5.12.1"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  }
}
