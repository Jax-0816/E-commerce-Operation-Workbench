{
  "name": "@eaw/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:contracts": "pnpm --filter @eaw/contracts run build",
    "predev": "pnpm run prepare:contracts",
    "dev": "vite",
    "prebuild": "pnpm run prepare:contracts",
    "build": "tsc -p tsconfig.json && vite build",
    "start": "vite preview --host 127.0.0.1",
    "pretest": "pnpm run prepare:contracts",
    "test": "vitest run src",
    "pretypecheck": "pnpm run prepare:contracts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src vite.config.ts"
  },
  "dependencies": {
    "@eaw/contracts": "workspace:*",
    "@eaw/shared": "workspace:*",
    "@fontsource-variable/noto-sans-sc": "5.3.0",
    "@phosphor-icons/react": "2.1.10",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-router-dom": "7.18.2"
  },
  "devDependencies": {
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.5",
    "jsdom": "27.0.0",
    "vite": "8.2.1"
  }
}
